package com.badlogic.gdx.backends;

import com.badlogic.gdx.Graphics;

/**
 *
 */
final class DefracDisplayMode extends Graphics.DisplayMode {
  protected DefracDisplayMode(int width, int height, int refreshRate, int bitsPerPixel) {
    super(width, height, refreshRate, bitsPerPixel);
  }
}
